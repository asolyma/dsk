overwrite: true
schema: "http://localhost:3000/api/graphql"
documents: null
generates:
  ./graphql/graphqltypes.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
